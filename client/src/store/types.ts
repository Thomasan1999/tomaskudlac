export enum ImageFormat {
    JPG = 'jpg',
    WEBP = 'webp',
}

export enum InitializingState {
    NOT_INITIALIZED,
    INITIALIZING,
    INITIALIZED,
}

export enum SiteLanguage {
    EN = 'en',
    SK = 'sk',
}
